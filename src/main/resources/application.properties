# Server Configuration
server.port=8080

# Spring Boot Admin (logging level)
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# Database configuration example (if using a database)
# spring.datasource.url=jdbc:mysql://localhost:3306/mutantdb
# spring.datasource.username=root
# spring.datasource.password=root